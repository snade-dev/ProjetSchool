# LEDGER — hypothèses prises faute d'information (à valider par le propriétaire)

Règle : l'exécutant ne s'arrête JAMAIS pour poser une de ces questions. Il applique l'hypothèse et note ici tout nouvel écart rencontré (ajouter une ligne, continuer).

| # | Sujet | Hypothèse appliquée | Impact si faux |
|---|---|---|---|
| H01 | Monnaie | FCFA, montants entiers (`Int`), format `1 250 000 FCFA` | Champs à passer en Decimal + formatage |
| H02 | Année scolaire | Septembre → Juin (l'axe des charts finance suit cet ordre) | Réordonner l'axe mensuel |
| H03 | Notation | /20, seuil de réussite = 10, mentions : ≥16 Excellent, ≥14 Bien, ≥12 Assez bien, ≥10 Passable | Constantes dans `src/lib/setting.ts` (GRADE_SCALE) — un seul endroit à changer |
| H04 | Multi-établissement | NON multi-tenant : une seule école par déploiement (`SchoolSettings` singleton id=1) | Refonte majeure (tenantId partout) — hors scope v2 |
| H05 | Rôle « comptable » | Pas de rôle dédié : l'admin gère la finance. `requireRole(["admin"])` partout en M4/M5 | Ajouter "accountant" dans routeAccessMap + guards (prévu par la conception, 1 story) |
| H06 | TVA / taxes | Aucune taxe sur les factures (écoles non assujetties) | Ajouter champ taux/ligne de taxe sur InvoiceLine |
| H07 | Échéance factures auto | Le 5 du mois facturé | Constante dans l'action `generateMonthlyInvoices` |
| H08 | Numérotation factures | `FAC-{année}-{compteur 5 chiffres}` global (max existant + 1, en transaction) | Changer le générateur de référence (isolé dans une fonction) |
| H09 | Salaires | Mensuels fixes (baseSalary), pas de paiement à l'heure ni de charges sociales calculées | Ajouter champs cotisations sur SalaryPayment |
| H10 | TuitionPayment existant | Données réelles présentes → migrées vers Invoice/Payment (S07), table conservée puis dépréciée ; montants Float arrondis à l'Int | Si table vide en prod : sauter le script, tout le reste inchangé |
| H11 | Devise des Float existants | `TuitionPayment.amount`, `Result.classScore` etc. restent tels quels ; seuls les NOUVEAUX modèles sont en Int | — |
| H12 | Présence | Le taux de présence = présents / pointages enregistrés (pas / jours ouvrés théoriques) | Ajuster la formule dans stats |
| H13 | Langue | UI 100 % français (le code, les identifiants et les commits restent en anglais) | i18n hors scope |
| H14 | Suppression d'élève | Cascade existante conservée : supprimer un élève supprime ses factures/paiements (Prisma `onDelete: Cascade`) — préférer la non-suppression en pratique | Passer en soft-delete (1 story dédiée) |
| H15 | Tests | Pas de framework de test installé → critères d'acceptation vérifiés manuellement + `pnpm build`. Aucune story n'installe vitest/jest sans demande explicite | Ajouter une story « harnais de tests » |
| H16 | proxy.ts (Next 16) | Le fichier `src/proxy.ts` remplace `middleware.ts` et est supposé fonctionnel ; S02 ajoute la défense en profondeur DANS les pages/actions sans dépendre du proxy | Si le proxy ne s'exécute pas, la sécurité reste assurée par les guards serveur |
| H17 | Bulletin — pondération | Moyenne matière = (classScore + score)/2 si les deux existent, sinon la note présente ; moyenne générale = moyenne simple des matières (pas de coefficients) | Ajouter `coefficient` sur Subject (1 story) |
| H18 | Employés ↔ Users | Les employés « staff » n'ont PAS de compte User (pas de connexion) | Créer des comptes staff (hors scope) |
| H19 | Helper dans le seed (S01) | `prisma/seed.ts` tourne via `ts-node` CommonJS sans `tsconfig-paths` → l'alias `@/` (utilisé transitivement par `src/lib/prisma.ts` que `getActiveSchoolYear` importe) n'est pas résolvable. Le seed réplique donc INLINE la logique du helper (`findFirst({where:{isActive:true}})` + throw `"Aucune année scolaire active — configurez /settings"`) au lieu de l'importer. Le helper reste disponible et utilisé par le code applicatif. | Installer `tsconfig-paths` et l'enregistrer dans le script seed pour importer réellement le helper (1 ligne de package.json) |
| H20 | Idempotence du seed existant (S01) | Le seed des `Subject` utilisait `create` (non idempotent, `name @unique`) → converti en `upsert` pour permettre les relances exigées par S01 | — |
| H21 | Rôles annonces/événements (S02) | La matrice §5 ne liste PAS explicitement Annonces/Événements. Appliqué `requireRole(["admin"])` sur toutes leurs mutations (règle « créer/maj/supprimer entités scolaires = admin »). Les `routeAccessMap` laissent la LECTURE ouverte à tous les rôles, ce qui reste cohérent. | Si les enseignants doivent créer annonces/événements : ajouter "teacher" à ces actions |
| H22 | Bug préexistant `updateAttendance` (S02) | `updateAttendance` (attendanceAction.ts) retourne `{success:false,error:true}` MÊME en cas de succès (le `return` après l'`update` est erroné d'origine). Sécurisé avec `requireRole(["admin","teacher"])` + ownership mais bug NON corrigé (hors scope S02, correction dans une story vie-scolaire). | Corriger le retour en `{success:true,error:false}` |
| H23 | Ownership bulk `updateResults` (S02) | `updateResults` (résultats en masse) et `updateMakeupExam` reçoivent une liste d'ids sans classe/matière : l'ownership enseignant granulaire par ligne n'est PAS vérifié (union `["admin","teacher"]` seulement). Le durcissement fin est prévu par S12 (écran gradeEntry). | Vérifier chaque id vs Lessons de l'enseignant dans S12 |
| H24 | Server actions en lecture seule (S02) | `getQuiz.ts`, `getQuizAction.ts`, `updateQuizScore.ts` sont des `"use server"` SANS mutation Prisma (findUnique) : laissés sans `requireRole` (hors scope « mutations » de S02). `updateQuizScore` ne persiste rien malgré son nom. | Ajouter un guard lecture si ces données deviennent sensibles |
| H25 | Relation `Employee.teacher` (S03) | La fiche demande la back-relation `employee Employee?` sur `Teacher` ; Prisma exige alors le champ opposé côté `Employee`. Ajouté `teacher Teacher? @relation(fields:[teacherId], references:[id], onDelete: SetNull)` (non listé littéralement dans architecture §2.2 mais implicite). SQL généré = uniquement `ALTER TABLE "Employee" ADD CONSTRAINT ... FK teacherId` (aucune perte de données). | Aucun — complète le §2.2 sans en changer la sémantique |
| H26 | `Expense.createdById` du seed (S03) | Les dépenses de démonstration utilisent `createdById: "seed"` (pas de FK contrainte sur ce champ — c'est un `User.id` libre, non relationnel dans le schéma). | Si une FK est ajoutée plus tard sur `createdById`, pointer vers un User admin réel |
| H27 | Filtre « mois » liste factures (S05) | Le filtre `?month=` porte sur le champ `Invoice.month` (rempli par la génération auto S06). Les factures manuelles ont `month=null` et n'apparaissent donc pas sous un filtre mois — cohérent avec la colonne « Mois » qui affiche `-` pour elles. | Filtrer plutôt sur `issueDate` (mois calendaire) si le besoin est « toutes les factures émises ce mois » |
| H28 | `deleteInvoiceLine` dernière ligne (S05) | Suppression refusée s'il ne reste qu'une ligne (une facture garde ≥ 1 ligne, cohérent avec `invoiceSchema.min(1)` et l'invariant total=Σlignes). Ajout aussi d'une action `deleteInvoice` (admin, refusée si paiement) NON exposée dans l'UI de S05 (on annule) mais présente pour garder la carte d'actions de `FormModal` typée correctement. | Autoriser la suppression de la dernière ligne / d'une facture vide si un autre workflow le requiert |
